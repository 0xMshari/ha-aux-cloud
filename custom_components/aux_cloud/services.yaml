get_power_consumption:
  name: Get power consumption
  description: >-
    Query power consumption (kWh) for one device over any date range.
    Returns the total and per-bucket values without changing the sensor period.
  fields:
    device_id:
      name: Device ID
      description: The AUX Cloud endpoint ID of the device.
      required: true
      example: "00000000000000000000907abe09717d"
      selector:
        text:
    start_date:
      name: Start date
      description: First day of the period (YYYY-MM-DD).
      required: true
      example: "2026-06-01"
      selector:
        date:
    end_date:
      name: End date
      description: Last day of the period (YYYY-MM-DD).
      required: true
      example: "2026-06-26"
      selector:
        date:
    report_type:
      name: Report type
      description: >-
        Optional bucket size (day, month, year). When omitted, the best
        granularity is chosen automatically from the date range.
      required: false
      example: day
      selector:
        select:
          options:
            - day
            - month
            - year

set_power_consumption_period:
  name: Set power consumption period
  description: >-
    Set the date range used by all Power Consumption sensors, then refresh them.
  fields:
    start_date:
      name: Start date
      description: First day of the period (YYYY-MM-DD).
      required: true
      example: "2026-06-01"
      selector:
        date:
    end_date:
      name: End date
      description: Last day of the period (YYYY-MM-DD).
      required: true
      example: "2026-06-26"
      selector:
        date:
